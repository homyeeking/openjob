# jobs

Scheduled Job CLI for registering global agent tasks under `~/.agents`.

```bash
npm install -g @homy/jobs
jobs init
jobs add path/to/JOB.md
```

See the project README for the full Jobs specification and examples.
