# TODO

> Tasks pending for this project.

| Status | Task | Priority | Notes |
|--------|------|----------|-------|
| [ ] | Scan the repository for old `jobs ...` CLI examples | Medium | Replace any remaining user-facing examples with `npx @homy/jobs@latest <command>` |
| [ ] | Review `cli/bin/jobs` and related CLI changes | High | Ensure current code changes match the documented invocation style |
| [ ] | Decide whether to prepare a commit for the current documentation and CLI updates | Low | Include `README.md`, `README.zh.md`, and `AGENTS.md` changes if needed |
